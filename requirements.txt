# Dependencies are managed via pyproject.toml.
# Run `uv sync` to install all dependencies, or:
#
#   uv sync                # runtime deps only
#   uv sync --dev          # + dev tools (pytest, pre-commit, ruff)
#   uv sync --all-extras   # all optional deps
#
# For pip-only installs:
#   pip install -e .                    # runtime
#   pip install -e ".[dev,doc,torch]"   # with extras
#
# Read the Docs installs from this file.
# Keep in sync with [project.optional-dependencies] doc in pyproject.toml.
sphinx
sphinx-rtd-theme
sphinx-tabs
sphinx-copybutton
#
